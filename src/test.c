#include <stdio.h>
#include "datatype.h"

int main(int argc, char const *argv[])
{
	int frequency=-1; // pulsos por segundo
	int rpm=-1; // revoliciones por minuto
	/*float ppr; // ppr pulsos por revolicion
	float ppp; // paso por pulso*/

	//frequency = rpm2hz(500);
	printf("\nfreq: %d",frequency);
	printf("\nrpm: %d",rpm);


	//rpm = hz2rpm(500);
	printf("\nfreq: %d",frequency);
	printf("\nrpm: %d",rpm);

	printf("\n");
	return 0;
}
